import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import ClientLayout from "@/components/ClientLayout";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Tech Zone Traders | Leading Digital Display & Advertising Solutions",
  description:
    "Tech Zone Traders provides professional LED display systems, sign board production, digital advertising equipment, and complete visual branding services in Nepal.",
  keywords:
    "LED display, sign board, digital advertising, branding solutions, Nepal, Tech Zone Traders",
  viewport: "width=device-width, initial-scale=1",
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ClientLayout>{children}</ClientLayout>
      </body>
    </html>
  );
}
